getLastName = ["Charlie", "Rob", "Andy"]

 function lastNameOnList (getLastName) {
    for (let i = 0; i < getLastName.length; i++){
        let name = []
        $name.append(i)}

    console.log ($name);
 }